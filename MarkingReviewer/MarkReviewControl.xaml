<UserControl x:Class="Chokanbar.MarkReviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="Auto"
             Width="Auto"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid  Background="White">
        <Button Grid.Row="1" Background="{Binding Path=BorderColor,Mode=OneWay}" MaxWidth="300" Margin="5,0,5,8" Click="JumpButtonClicked">
            <TextBlock Text="{Binding Path=Text,Mode=OneWay}" TextWrapping="Wrap" />
        </Button>
    </Grid>
</UserControl>
