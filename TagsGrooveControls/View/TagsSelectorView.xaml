<UserControl x:Class="TagsGrooveControls.View.TagsSelectorView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:TagsGrooveControls="clr-namespace:TagsGrooveControls.View" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="タグツリー"/>
        <TagsGrooveControls:TagsGrooveTreeView x:Name="_treeView" Grid.Column="0" Grid.Row="1"/>
        <Button Grid.Column="1"  Grid.Row="1" Content="→"/>
        <Label Grid.Row="0" Grid.Column="2" Content="選択中のタグ"/>
        <Border Grid.Column="2" Grid.Row="1">
            <ListBox Grid.Row="1">
                    
            </ListBox>
        </Border>

    </Grid>
</UserControl>
